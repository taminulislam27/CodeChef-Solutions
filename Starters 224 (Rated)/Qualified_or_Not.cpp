#include <bits/stdc++.h>
using namespace std;

int main()
{
    // your code goes here
    int a, b, c;
    cin >> a >> b >> c;
    (a >= 2 * b && a >= 2 * c) ? cout << "YES" : cout << "NO";
}